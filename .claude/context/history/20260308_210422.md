# Context Snapshot
**Saved:** 2026-03-08
**Phase:** Scraper development + testing
**Branch:** main

## What Was Accomplished
- Created `tests/test_scrapers/test_scraper.py` — 20 tests (19 pass, 1 skipped) covering:
  - Rakuten unit tests (mocked HTTP): genre filter, schema, pagination, error handling
  - Rakuten live API smoke test (skipped without `RAKUTEN_APP_ID`, runs in GitHub Actions)
  - PCKoubou Playwright browser availability check + live scrape smoke test
  - Sofmap parsing unit tests (price text, product ID extraction)
- Cleaned up `.github/workflows/scraper.yaml`:
  - Removed unused deps: `pandas`, `streamlit`, `mojimoji`
  - Added `playwright` package + `playwright install chromium --with-deps` step (was missing, breaking PCKoubou/Sofmap in CI)
- Pushed both commits to GitHub (`origin/main`)
- Created `src/kakakucom_scrape.py` — full Kakaku.com used-PC scraper:
  - `parse_kakaku_listings(html, search_query)` — pure bs4 parser for `.tblUsed` table
  - `scrape_kakaku(key, url)` — async Playwright fetcher
  - `run_kakaku_scraper()` — sync entry point for all 8 queries
  - `SAMPLE_HTML` embedded for testing
- Created `tests/test_scrapers/test_kakaku_scraper.py` — 25 tests, all passing
- Ran `Marketprice2.py` via streamlit on port 8502

## Current State
- **Working on:** Kakaku.com scraper (newly created, not yet integrated into `scraper.py`)
- **Blocked by:** None
- **Files modified this session:**
  - `tests/test_scrapers/test_scraper.py` (committed + pushed)
  - `.github/workflows/scraper.yaml` (committed + pushed)
  - `src/kakakucom_scrape.py` (new, **uncommitted**)
  - `tests/test_scrapers/test_kakaku_scraper.py` (new, **uncommitted**)
- **Tests status:** All passing — 25/25 kakaku, 19+1skip/20 scraper suite
- **GitHub Actions:** Pushed, user was about to trigger the workflow to validate playwright fix

## Key Decisions Made
- Used `beautifulsoup4` for kakaku.com HTML parsing (`selectolax` not installed, `bs4` confirmed available)
- Kakaku scraper uses Playwright to fetch pages (same pattern as PCKoubou/Sofmap)
- `parse_kakaku_listings` is a pure function — fully unit-testable with static HTML
- `SAMPLE_HTML` (3 real L390 listings) embedded directly in `kakakucom_scrape.py`
- Sofmap NOT added to live tests — anti-bot measures make live scraping unreliable
- Workflow deps trimmed to: `polars supabase requests pytz playwright` only

## Next Steps (Priority Order)
1. Commit `src/kakakucom_scrape.py` and `tests/test_scrapers/test_kakaku_scraper.py`
2. Add `beautifulsoup4` to workflow deps in `scraper.yaml` (needed for kakaku parser in CI)
3. Integrate kakaku scraper into `scraper.py` (`run_scraper()` function) with Supabase upsert
4. Decide: does kakaku data go into existing `products`/`price_history` tables, or separate? Kakaku has extra fields (rank, listedDate) not in current schema
5. Check GitHub Actions run result after playwright fix
6. `tests/test_features/test_model_extractor.py` references `src.features.model_extractor` which doesn't exist — implement or delete

## Open Questions
- Should kakaku data share the existing DB schema or have its own table? (rank/listedDate fields don't exist currently)
- Did the GitHub Actions workflow pass after adding playwright browser install?

## Important Notes
- `scraper.yaml` workflow name is "Rust-Fast PC Scraper 1" (user renamed it)
- `Marketprice2.py` fetches from `listings_view` (a DB view); `Marketprice.py` fetches from `products` + `price_history` directly
- `tests/test_features/test_model_extractor.py` will crash pytest with `-x` — `src.features.model_extractor` does not exist
- Never touch `.env` or `secrets.toml`
- Always use Polars, never pandas
- `bs4` is installed, `selectolax` is NOT installed
- `price_history` column is `scraped_at` (NOT `observed_at`)

## File Map (this session)
```
tests/test_scrapers/test_scraper.py          — scraper test suite (committed)
tests/test_scrapers/test_kakaku_scraper.py   — 25 kakaku tests (uncommitted)
src/kakakucom_scrape.py                      — kakaku scraper + parser (uncommitted)
.github/workflows/scraper.yaml              — cleaned deps + playwright (committed)
```
