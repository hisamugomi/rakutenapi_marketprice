# Context Snapshot
**Saved:** 2026-03-09
**Phase:** Scraper development — all scrapers integrated, tests passing
**Branch:** main

## What Was Accomplished
- Added 10 integration tests to `tests/test_scrapers/test_kakaku_scraper.py` (Polars conversion, column mapping, price_history readiness, Playwright check, live scrape) — 35 total, all passing
- Created `src/pcwrapscrape.py`: refactored from root `pcwrapscrape.py` into `src/`
  - Separated `parse_pcwrap_listings(html)` (pure BeautifulSoup parser) from Playwright fetcher
  - Aligned output field names to `_PRODUCT_COLS`: `memory`, `ssd`, `hdd`, `display_size`, `shopName`, `model` added
  - Added `SAMPLE_HTML` with 3 representative items (SSD, SSD, HDD)
- Created `tests/test_scrapers/test_pcwrap_scraper.py` — 37 tests, all passing including live scrape
- Wired `run_pcwrap_scraper` into `scraper.py` (no `with_columns` needed — fields pre-aligned)
- Wired `run_kakaku_scraper` into `scraper.py` (with `with_columns` for ram→memory, storage→ssd, screen→display_size, search_query→model)
- Added `beautifulsoup4` to `.github/workflows/scraper.yaml` pip install
- Created `SCRAPER_GUIDE.md` — authoring manual for adding new scrapers
- Committed and pushed everything to `origin/main` (commit `c4d59f2`)

## Current State
- **Working on:** Nothing — session complete, pushed to GitHub
- **Blocked by:** None
- **Files modified this session:**
  - `src/pcwrapscrape.py` (new, committed)
  - `src/kakakucom_scrape.py` (new, committed)
  - `tests/test_scrapers/test_pcwrap_scraper.py` (new, committed)
  - `tests/test_scrapers/test_kakaku_scraper.py` (new, committed)
  - `scraper.py` (modified, committed)
  - `.github/workflows/scraper.yaml` (modified, committed)
  - `SCRAPER_GUIDE.md` (new, committed)
- **Tests status:** 35/35 kakaku, 37/37 pcwrap — all passing
- **GitHub Actions:** Pushed to main, user was about to trigger manual run

## Key Decisions Made
- Kakaku fields need `with_columns` in scraper.py: `ram→memory`, `storage→ssd`, `screen→display_size` (strips "インチ"), `search_query→model`
- PCwrap fields are pre-aligned in the scraper itself — no transforms needed in scraper.py
- `display_size` for kakaku: Polars `.str.replace("インチ","").cast(pl.Float64, strict=False)`
- `display_size` for pcwrap: already a `float` from `_parse_specs()` regex
- PCwrap `storage_type`: routes to `ssd` or `hdd` column based on value ("HDD" → hdd, else → ssd)
- PCwrap `shopName` hardcoded to `"PCwrap"` (it's a direct retailer, no per-item shop)
- `rank`/`listedDate` from kakaku silently dropped by `_upsert_batch`'s `available_cols` filter
- Old `pcwrapscrape.py` in root still exists (not deleted, user aware)
- Streamlit Cloud reads `requirements.txt` from repo root (separate from `pyproject.toml` used by uv locally)

## Next Steps (Priority Order)
1. Check GitHub Actions run result (commit `c4d59f2`) — verify all 5 scrapers run without error
2. Delete old `pcwrapscrape.py` from repo root (replaced by `src/pcwrapscrape.py`)
3. Address `tests/test_features/test_model_extractor.py` — imports `src.features.model_extractor` which doesn't exist; will crash pytest with `-x`
4. Consider updating `requirements.txt` if any new deps are needed for the Streamlit dashboard
5. Begin ML pipeline phase (LightGBM price model, survival model) per project plan

## Open Questions
- Did the GitHub Actions workflow pass with all 5 scrapers? (user was about to trigger)
- Should `rank` (condition grade from kakaku) be added to the DB schema? Currently dropped.
- `src/sofmapscrape_copy.py` is untracked — what is it? Probably safe to delete.

## Important Notes
- `scraper.py` pipeline order: Rakuten → PCKoubou → PCwrap → Kakaku → Sofmap
- `_upsert_batch` rename map: `itemCode→item_code`, `itemName→item_name`, `itemUrl→item_url`, `shopName→shop_name`
- `_PRODUCT_COLS` in scraper.py (what gets sent to Supabase products table):
  `item_code, source, item_name, item_url, shop_name, search_query, brand, model, cpu, cpu_gen, memory, ssd, hdd, os, display_size, weight, bluetooth, webcam, usb_ports, is_active, last_seen_at`
- `price_history` uses: `product_id`, `item_code`, `source`, `price` (from itemPrice), `scraped_at`, `search_query`
- `price_history` column is `scraped_at` (NOT `observed_at`)
- Never touch `.env` or `secrets.toml`
- Always use Polars, never pandas
- `bs4` is installed, `selectolax` is NOT installed
- `SCRAPER_GUIDE.md` documents the full checklist for adding new scrapers

## File Map
```
src/kakakucom_scrape.py              — kakaku scraper + parser (committed)
src/pcwrapscrape.py                  — pcwrap scraper + parser (committed, moved from root)
pcwrapscrape.py                      — OLD root file, not yet deleted
tests/test_scrapers/test_kakaku_scraper.py   — 35 tests (committed)
tests/test_scrapers/test_pcwrap_scraper.py   — 37 tests (committed)
tests/test_scrapers/test_scraper.py          — 20 tests rakuten/pckoubou/sofmap (committed earlier)
scraper.py                           — main pipeline, all 5 scrapers wired (committed)
.github/workflows/scraper.yaml       — beautifulsoup4 added (committed)
SCRAPER_GUIDE.md                     — new scraper authoring manual (committed)
```
